import {TestBed} from '@angular/core/testing';

import {UpdateEinsatzService} from './update-einsatz.service';

describe('UpdateEinsatzService', () => {
  let service: UpdateEinsatzService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(UpdateEinsatzService);
  });

  //it('should be created', () => {
  //  expect(service).toBeTruthy();
  //});
});
