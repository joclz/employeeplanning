import {TestBed} from '@angular/core/testing';

import {UpdateMitarbeiterVertriebService} from './update-mitarbeiter-vertrieb.service';

describe('UpdateMitarbeiterVertriebService', () => {
  let service: UpdateMitarbeiterVertriebService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(UpdateMitarbeiterVertriebService);
  });

  //it('should be created', () => {
  //  expect(service).toBeTruthy();
  //});
});
